name: Build Veloera

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-freebsd:
    runs-on: ubuntu-latest
    name: Build Veloera on FreeBSD
    steps:
      - name: Get version
        id: get_version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "VERSION=${{ github.event.inputs.tag_name }}" >> $GITHUB_ENV
          else
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Clone Veloera Repo
        uses: actions/checkout@v4
        with:
          repository: Veloera/Veloera
          ref: ${{ env.VERSION }}
          path: Veloera

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false
      
      - name: Build Frontend
        env:
          CI: ""
        run: |
          cd Veloera/web
<<<<<<< HEAD
          pnpm install
          VITE_REACT_APP_VERSION=${{ env.VERSION }} pnpm run build
=======
          npm install --legacy-peer-deps
          VITE_REACT_APP_VERSION=$(git describe --tags) npm run build
>>>>>>> d2301fe4393d7bef8d0e4e8526f9d7ce6356e5fe
          cd ../..

      - name: Build Backend
        uses: vmactions/freebsd-vm@v1
        with:
          usesh: true
          release: 13.2
          prepare: |
            pkg install -y wget git gcc
            wget https://dl.google.com/go/go1.22.1.freebsd-amd64.tar.gz && tar -C /usr/local -xzf go1.22.1.freebsd-amd64.tar.gz && rm go1.22.1.freebsd-amd64.tar.gz
            ln -s /usr/local/go/bin/go /usr/local/bin/go
          run: |
            cd Veloera
            go mod download
            go build -ldflags "-s -w -X 'veloera/common.Version=${{ env.VERSION }}' -extldflags '-static'" -o veloera

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: veloera-freebsd
          path: Veloera/veloera
          retention-days: 1
  
      - name: Create release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.VERSION }}
          files: Veloera/veloera
          generate_release_notes: true
              
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 1
          keep_minimum_runs: 8
